# LLM provider
ANTHROPIC_API_KEY=

# Postgres — Flue persistence + the app store (any Postgres 15+)
DATABASE_URL=postgres://localhost:5432/super_inbox_dev

# Slack app (Basic Information → Signing Secret; OAuth → Bot User OAuth Token)
SLACK_SIGNING_SECRET=
SLACK_BOT_TOKEN=
